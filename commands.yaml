run:
  switch:
    docs:
      script: |
        cd website
        cp ../CHANGELOG.md ./src/pages/changelog.md
        npm run start
    tests:
      script: |
        dart test --test-randomize-ordering-seed=random
        cd example && dart test
