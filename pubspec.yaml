name: commands_cli
description: A powerful CLI tool that generates type-safe, project-local command wrappers from YAML definitions for Dart and Flutter developers.
version: 0.3.2
repository: https://github.com/Nikoro/commands_cli
issue_tracker: https://github.com/Nikoro/commands_cli/issues
homepage: https://nikoro.github.io/commands_cli
documentation: https://nikoro.github.io/commands_cli/docs
topics: [cli, makefile, commands, task-runner, script-manager]

environment:
  sdk: ^3.0.0

executables:
  commands: commands

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0

